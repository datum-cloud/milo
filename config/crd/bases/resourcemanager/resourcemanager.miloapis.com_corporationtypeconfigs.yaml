---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: corporationtypeconfigs.resourcemanager.miloapis.com
spec:
  group: resourcemanager.miloapis.com
  names:
    categories:
    - datum
    kind: CorporationTypeConfig
    listKind: CorporationTypeConfigList
    plural: corporationtypeconfigs
    singular: corporationtypeconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.active
      name: Active
      type: boolean
    - jsonPath: .status.activeTypeCount
      name: Type Count
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CorporationTypeConfig is the Schema for the CorporationTypeConfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CorporationTypeConfigSpec defines the desired state of CorporationTypeConfig
            properties:
              active:
                default: true
                description: Whether this configuration is active
                type: boolean
              corporationTypes:
                description: Available corporation types that can be selected for
                  vendors
                items:
                  description: CorporationTypeDefinition defines a single corporation
                    type option
                  properties:
                    code:
                      description: The unique identifier for this corporation type
                      pattern: ^[a-z0-9-]+$
                      type: string
                    description:
                      description: Optional description of this corporation type
                      type: string
                    displayName:
                      description: Human-readable display name
                      type: string
                    enabled:
                      default: true
                      description: Whether this corporation type is currently available
                        for selection
                      type: boolean
                    sortOrder:
                      default: 100
                      description: Sort order for display purposes (lower numbers
                        appear first)
                      format: int32
                      type: integer
                  required:
                  - code
                  - displayName
                  - enabled
                  - sortOrder
                  type: object
                minItems: 1
                type: array
            required:
            - active
            - corporationTypes
            type: object
          status:
            description: CorporationTypeConfigStatus defines the observed state of
              CorporationTypeConfig
            properties:
              activeTypeCount:
                description: Number of active corporation types
                format: int32
                type: integer
              conditions:
                default:
                - lastTransitionTime: "1970-01-01T00:00:00Z"
                  message: Waiting for control plane to reconcile
                  reason: Unknown
                  status: Unknown
                  type: Ready
                description: |-
                  Conditions represents the observations of a corporation type config's current state.
                  Known condition types are: "Ready"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this CorporationTypeConfig by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

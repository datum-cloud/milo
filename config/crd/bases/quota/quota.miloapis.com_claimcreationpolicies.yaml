---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: claimcreationpolicies.quota.miloapis.com
spec:
  group: quota.miloapis.com
  names:
    kind: ClaimCreationPolicy
    listKind: ClaimCreationPolicyList
    plural: claimcreationpolicies
    singular: claimcreationpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetResource.apiVersion/.spec.targetResource.kind
      name: Target
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.resourceClaimsCreated
      name: Created Claims
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClaimCreationPolicy is the Schema for the claimcreationpolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClaimCreationPolicySpec defines the desired state of ClaimCreationPolicy.
            properties:
              enabled:
                default: true
                description: |-
                  Enabled determines if this policy is active.
                  If false, no ResourceClaims will be created for matching resources.
                type: boolean
              resourceClaimTemplate:
                description: ResourceClaimTemplate defines how to create ResourceClaims
                  for the target resource.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations to add to the created ResourceClaim.
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to add to the created ResourceClaim.
                    type: object
                  nameTemplate:
                    description: |-
                      NameTemplate for generating ResourceClaim names.
                      Supports Go templating with variables like {{.ResourceName}}, {{.Namespace}}, {{.Kind}}.
                      If not specified, a default template will be used.
                    type: string
                  namespace:
                    description: |-
                      Namespace where the ResourceClaim should be created.
                      If not specified, defaults to the milo-system namespace.
                    type: string
                  requests:
                    description: |-
                      Requests defines the resource requests to include in the ResourceClaim.
                      Multiple requests enable claiming different resource types simultaneously.
                    items:
                      description: |-
                        ResourceRequestTemplate defines how to create individual resource requests.
                        Supports both static values and CEL expressions using field name suffixes.
                      properties:
                        amount:
                          description: |-
                            Amount is the quota amount to request (static value).
                            Mutually exclusive with AmountExpression.
                          format: int64
                          type: integer
                        amountExpression:
                          description: |-
                            AmountExpression - CEL expression to calculate amount dynamically.
                            Mutually exclusive with Amount.
                          type: string
                        conditionExpression:
                          description: |-
                            ConditionExpression - CEL expression to determine if this request should be created.
                            If empty or evaluates to true, the request is included.
                          type: string
                        dimensionExpressions:
                          additionalProperties:
                            type: string
                          description: |-
                            DimensionExpressions - CEL expressions for dynamic dimension values.
                            Merged with static Dimensions, expressions take precedence for duplicate keys.
                          type: object
                        dimensions:
                          additionalProperties:
                            type: string
                          description: |-
                            Dimensions for the resource claim (static key-value pairs).
                            Can be combined with DimensionExpressions.
                          type: object
                        resourceType:
                          description: |-
                            ResourceType to use in the ResourceClaim.
                            Must correspond to an active ResourceRegistration.
                          minLength: 1
                          type: string
                      required:
                      - resourceType
                      type: object
                    minItems: 1
                    type: array
                required:
                - requests
                type: object
              targetResource:
                description: |-
                  TargetResource specifies which resource type this policy applies to.
                  Each ClaimCreationPolicy targets exactly one resource type (GVK).
                properties:
                  apiVersion:
                    description: APIVersion of the target resource in the format "group/version".
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/v[0-9]+((alpha|beta)[0-9]*)?$
                    type: string
                  kind:
                    description: Kind is the kind of the target resource.
                    minLength: 1
                    type: string
                required:
                - apiVersion
                - kind
                type: object
            required:
            - resourceClaimTemplate
            - targetResource
            type: object
          status:
            description: ClaimCreationPolicyStatus defines the observed state of ClaimCreationPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the policy's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastResourceClaimCreated:
                description: LastResourceClaimCreated is the timestamp of the most
                  recent ResourceClaim creation.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed.
                format: int64
                type: integer
              resourceClaimsCreated:
                description: ResourceClaimsCreated is the total number of ResourceClaims
                  created by this policy.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

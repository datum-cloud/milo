---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: allowancebuckets.quota.miloapis.com
spec:
  group: quota.miloapis.com
  names:
    kind: AllowanceBucket
    listKind: AllowanceBucketList
    plural: allowancebuckets
    singular: allowancebucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.resourceTypeName
      name: Resource Type
      type: string
    - jsonPath: .status.allocated
      name: Allocated
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AllowanceBucket is the Schema for the allowancebuckets API.
          Provides the single source of truth for usage accounting for specific resource and dimension combinations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AllowanceBucketSpec defines the desired state of AllowanceBucket.
            properties:
              dimensions:
                additionalProperties:
                  type: string
                description: Dimensions for this bucket as key-value pairs
                type: object
              ownerRef:
                description: Reference to the EffectiveResourceGrant that owns this
                  bucket
                properties:
                  apiGroup:
                    description: API group of the resource
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: Kubernetes resource type - Must be PascalCase
                    pattern: ^[A-Z][a-zA-Z]*$
                    type: string
                  name:
                    description: Name of the specific resource instance
                    type: string
                  uid:
                    description: UID of the specific resource instance
                    type: string
                required:
                - apiGroup
                - kind
                type: object
              resourceTypeName:
                description: The resource type this bucket tracks quota usage for
                pattern: ^[a-z]([-a-z]*[a-z])?(\.[a-z]([-a-z]*[a-z])?)*\/[a-zA-Z][a-zA-Z]*(\/*[a-zA-Z][a-zA-Z]*)*$
                type: string
            required:
            - ownerRef
            - resourceTypeName
            type: object
          status:
            description: AllowanceBucketStatus defines the observed state of AllowanceBucket.
            properties:
              allocated:
                description: Amount of quota currently allocated/used in this bucket
                format: int64
                minimum: 0
                type: integer
              allocations:
                description: A list of all the claims that have been allocated quota
                  from this bucket.
                items:
                  description: Allocation represents a single allocation of quota
                    from a bucket to a claim.
                  properties:
                    amount:
                      description: |-
                        The amount of quota allocated.
                        /
                      format: int64
                      minimum: 0
                      type: integer
                    claimRef:
                      description: Reference to the granted ResourceClaim that this
                        allocation is for.
                      properties:
                        apiGroup:
                          description: API group of the resource
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                        kind:
                          description: Kubernetes resource type - Must be PascalCase
                          pattern: ^[A-Z][a-zA-Z]*$
                          type: string
                        name:
                          description: Name of the specific resource instance
                          type: string
                        uid:
                          description: UID of the specific resource instance
                          type: string
                      required:
                      - apiGroup
                      - kind
                      type: object
                  required:
                  - amount
                  - claimRef
                  type: object
                type: array
              observedGeneration:
                description: The specific revision of the AllowanceBucket
                format: int64
                type: integer
            required:
            - allocated
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

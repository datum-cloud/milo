---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: effectiveresourcegrants.quota.miloapis.com
spec:
  group: quota.miloapis.com
  names:
    kind: EffectiveResourceGrant
    listKind: EffectiveResourceGrantList
    plural: effectiveresourcegrants
    singular: effectiveresourcegrant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.resourceTypeName
      name: Resource Type
      type: string
    - jsonPath: .status.totalLimit
      name: Total Limit
      type: integer
    - jsonPath: .status.totalAllocated
      name: Allocated
      type: integer
    - jsonPath: .status.available
      name: Available
      type: integer
    - jsonPath: .status.allowanceBucketRefs[*].name
      name: Buckets
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EffectiveResourceGrant is the Schema for the effectiveresourcegrants
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EffectiveResourceGrantSpec defines the desired state of EffectiveResourceGrant.
            properties:
              ownerRef:
                description: Reference to the resource that owns the effective grant.
                properties:
                  apiGroup:
                    description: API group of the resource
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: Kubernetes resource type - Must be PascalCase
                    pattern: ^[A-Z][a-zA-Z]*$
                    type: string
                  name:
                    description: Name of the specific resource instance
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              resourceTypeName:
                description: The resource type this effective grant aggregates quota
                  information for
                pattern: ^[a-z]([-a-z]*[a-z])?(\.[a-z]([-a-z]*[a-z])?)*\/[a-zA-Z][a-zA-Z]*(\/*[a-zA-Z][a-zA-Z]*)*$
                type: string
            required:
            - ownerRef
            - resourceTypeName
            type: object
          status:
            description: EffectiveResourceGrantStatus defines the observed state of
              EffectiveResourceGrant.
            properties:
              allowanceBucketRefs:
                description: References to AllowanceBuckets used to calculate totalAllocated
                items:
                  description: AllowanceBucketRef references an AllowanceBucket and
                    its observed state
                  properties:
                    allocated:
                      description: Amount allocated in this bucket
                      format: int64
                      minimum: 0
                      type: integer
                    name:
                      description: Name of the AllowanceBucket
                      type: string
                    observedGeneration:
                      description: |-
                        The generation of the AllowanceBucket when this reference was last
                        observed
                      format: int64
                      type: integer
                  required:
                  - allocated
                  - name
                  type: object
                type: array
              available:
                description: |-
                  The amount available that can be claimed
                  Available = (totalLimit - totalAllocated)
                format: int64
                minimum: 0
                type: integer
              observedGeneration:
                description: The specific revision of the EffectiveResourceGrant
                format: int64
                type: integer
              totalAllocated:
                description: Total allocated usage across all AllowanceBucket resources
                  for this resource type
                format: int64
                minimum: 0
                type: integer
              totalLimit:
                description: Total effective quota limit from all applicable ResourceGrants
                  for this resource type
                format: int64
                minimum: 0
                type: integer
            required:
            - available
            - totalAllocated
            - totalLimit
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

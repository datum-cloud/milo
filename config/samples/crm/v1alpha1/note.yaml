apiVersion: crm.miloapis.com/v1alpha1
kind: Note
metadata:
  name: demo-note-01
spec:
  # Who / what the note is ABOUT
  subjectRef:
    apiGroup: iam.miloapis.com   # or notification.miloapis.com
    kind: User                  # or Contact
    name: note-test             # target resource name
    namespace: default          # only required for namespace-scoped resources

  # Main text of the note (≤ 1000 chars)
  content: |
    Alice gave a product demo on 2025-11-18.
    Very positive feedback, but she requested a pricing breakdown.

  # Optional metadata
  interactionTime: "2025-11-18T14:30:00Z"   # when the interaction happened
  nextAction: "Send pricing details"         # follow-up item
  nextActionTime: "2025-11-22T09:00:00Z"     # when to do the follow-up

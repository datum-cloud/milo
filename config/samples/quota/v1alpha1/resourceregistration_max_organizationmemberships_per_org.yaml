apiVersion: quota.miloapis.com/v1alpha1
kind: ResourceRegistration
metadata:
  name: registration-def456
spec:
  ownerTypeRef:
    apiGroup: resourcemanager.miloapis.com
    kind: Organization
  # Entity type for countable resources like projects
  type: "Entity"
  # Fully qualified name of the resource type being registered
  resourceType: "resourcemanager.miloapis.com/OrganizationMembership"
  # Description of the quota limit for the resource type
  description: "Maximum number of OrganizationMembership resources that can be created within an Organization."
  # The base unit of measurement for the resource
  baseUnit: "OrganizationMembership"
  # The pluralized unit of measurement UI will display to users
  displayUnit: "Organization Memberships"
  # Defines how to convert between the base unit and the display unit
  # Formula: baseValue / unitConversionFactor = displayValue
  unitConversionFactor: 1
  # Dimensions that can be used in ResourceGrant selectors
  # No dimensions as this is just a very simple entity-based use case
  dimensions: [] 
apiVersion: iam.miloapis.com/v1alpha1
kind: Role
metadata:
  name: crm-note-admin
  annotations:
    kubernetes.io/display-name: CRM Note Admin
    kubernetes.io/description: "Full administrative access to notes attached to subjects."
spec:
  launchStage: Beta
  inheritedRoles:
    - name: crm-note-editor

apiVersion: iam.miloapis.com/v1alpha1
kind: Role
metadata:
  name: crm-note-creator
  annotations:
    kubernetes.io/display-name: CRM Note Creator
    kubernetes.io/description: "Allows creating notes attached to subjects."
spec:
  launchStage: Beta
  includedPermissions:
    - crm.miloapis.com/notes.create
